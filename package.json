{
  "name": "rule-based-server",
  "version": "1.0.0",
  "private": true,
  "description": "Rule-based API abuse detection server with latency-budgeted hybrid escalation to an AI detector. Built for the dissertation 'Experimental Analysis of Latency-Accuracy Trade-offs in Real-Time API Abuse Detection Systems'.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "echo \"No linter configured\" && exit 0",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "uuid": "^11.0.5"
  },
  "devDependencies": {}
}
